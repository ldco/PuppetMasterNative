# PMNative (Expo) environment variables
# Copy to your local env workflow (.env, .env.local, or Expo secrets) as needed.

# Default backend provider: Supabase
EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# PMN-021 Google social auth smoke test notes:
# - Enable backend.socialAuth.google = true in src/pm-native.config.ts
# - Google provider client ID/secret are configured in Supabase dashboard (not in this file)
# - Redirect/callback URLs are configured in Supabase Auth URL settings (see docs/SUPABASE_GOOGLE_SOCIAL_AUTH_SMOKE_TEST_SETUP.md)

# Optional: used by generic-rest backend calls and chatbot proxy mode (including Supabase Edge Functions)
EXPO_PUBLIC_API_BASE_URL=https://api.your-domain.com

# Optional: chatbot UX demo/live model settings (Assistant tab)
# Keep the placeholder to run local mock-mode blocks (quick replies/menu/forms).
# Preferred production mode: set a backend proxy path and keep model keys server-side.
# EXPO_PUBLIC_CHATBOT_PROXY_PATH=/chatbot-complete
# EXPO_PUBLIC_CHATBOT_ALLOW_DIRECT=false
EXPO_PUBLIC_CHATBOT_API_KEY=PASTE_YOUR_API_KEY_HERE
EXPO_PUBLIC_CHATBOT_MODEL=gpt-5.2-mini
EXPO_PUBLIC_CHATBOT_API_ENDPOINT=https://api.openai.com/v1/responses
